<?xml version="1.0" encoding="utf-8"?>
<androidx.appcompat.widget.Toolbar
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_height="wrap_content"
    android:layout_width="match_parent"
    app:navigationIcon="@drawable/ic_baseline_menu"
    app:collapseIcon="@drawable/ic_baseline_menu_open"
    app:theme="@style/Theme.SafePassword"
    android:fitsSystemWindows="true"
    android:minHeight="?attr/actionBarSize"
    android:background="@color/color_primary_dark"
    android:id="@+id/main_toolbar">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

            <TextView
                android:id="@+id/toolbar_title"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_centerInParent="true"
                android:layout_alignParentEnd="true"
                android:autoSizeTextType="uniform"
                android:layout_marginEnd="@dimen/padding_button"
                android:textSize="@dimen/toolbar_name"
                android:textStyle="bold"
                android:text="@string/app_name"
                android:textColor="@color/color_on_primary"
                android:maxLines="@integer/item_max_lines"/>
    </RelativeLayout>

</androidx.appcompat.widget.Toolbar>